<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.expressway.mapper.SysDeviceMapper">
    <!-- 查询所有设备 -->
    <select id="selectAllDevice" resultType="com.expressway.entity.SysDevice">
        SELECT * FROM sys_device ORDER BY create_time DESC
    </select>

    <!-- 根据区域ID查询设备 -->
    <select id="selectDeviceByAreaId" resultType="com.expressway.entity.SysDevice">
        SELECT * FROM sys_device WHERE area_id = #{areaId} ORDER BY create_time DESC
    </select>

    <!-- 根据ID查询设备 -->
    <select id="selectDeviceById" resultType="com.expressway.entity.SysDevice">
        SELECT * FROM sys_device WHERE id = #{id}
    </select>

    <!-- 根据设备编码查询设备 -->
    <select id="selectDeviceByCode" resultType="com.expressway.entity.SysDevice">
        SELECT * FROM sys_device WHERE device_code = #{deviceCode}
    </select>

    <!-- 新增设备 -->
    <insert id="insertDevice" parameterType="com.expressway.entity.SysDevice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_device (device_name, device_code, device_type, model, area_id, install_location, ip_address, status, create_time, update_time)
        VALUES (#{deviceName}, #{deviceCode}, #{deviceType}, #{deviceModel}, #{areaId}, #{location}, #{ipAddress}, #{status}, NOW(), NOW())
    </insert>

    <!-- 更新设备 -->
    <update id="updateDeviceById" parameterType="com.expressway.entity.SysDevice">
        UPDATE sys_device
        SET device_name = #{deviceName},
            device_code = #{deviceCode},
            device_type = #{deviceType},
            model = #{deviceModel},
            area_id = #{areaId},
            install_location = #{location},
            ip_address = #{ipAddress},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除设备 -->
    <delete id="deleteDeviceById">
        DELETE FROM sys_device WHERE id = #{id}
    </delete>

    <!-- 批量删除设备 -->
    <delete id="batchDeleteDevice">
        DELETE FROM sys_device WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
