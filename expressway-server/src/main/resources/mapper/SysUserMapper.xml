<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expressway.mapper.SysUserMapper">

    <!-- 查询用户列表 -->
    <select id="selectUserList" parameterType="com.expressway.dto.UserQueryParamsDTO"
            resultType="com.expressway.vo.UserVO">
        SELECT u.*, r.role_name, d.dept_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        LEFT JOIN sys_dept d ON r.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                AND u.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="roleName != null and roleName != ''">
                AND r.role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
            <if test="deptName != null and deptName != ''">
                AND d.dept_name LIKE CONCAT('%', #{deptName}, '%')
            </if>
        </where>
    </select>

    <!-- 新增用户 -->
    <insert id="insert" parameterType="com.expressway.entity.SysUser">
        INSERT INTO sys_user (name, gender, username, password, phone, id_card, role_id, remark, create_time,
        update_time)
        VALUES (#{name}, #{gender}, #{username}, #{password}, #{phone}, #{idCard}, #{roleId}, #{remark}, NOW(), NOW())
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.expressway.dto.UserUpdateDTO">
        UPDATE sys_user
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

    <!-- 批量删除用户 -->
    <delete id="batchDelete" parameterType="java.util.List">
        DELETE FROM sys_user WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
